fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-multipart==0.0.9
PyJWT==2.8.0
httpx==0.27.0
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
xgboost==2.0.3
joblib==1.4.2
python-dotenv==1.0.1
# Image processing (resize before OCR, magic-byte validation)
Pillow>=10.0.0
# PaddleOCR — install separately (large binary, ~1 GB):
#   pip install paddlepaddle paddleocr
# Then set PADDLE_OCR_ENABLED=true in .env (default: true, falls back to mock if not installed)
